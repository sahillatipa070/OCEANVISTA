import { Waves,Calendar,ChevronDown } from 'lucide-react'; import {useOceanStore,times} from '../store/oceanStore';
const vars=['Temperature (°C)','Salinity (PSU)','Chlorophyll (mg/m³)','Ocean Current Speed','Dissolved Oxygen'];
export default function Header(){const s=useOceanStore(); return <header className="header"><div className="brand"><Waves/><div><b>OCEANVISTA 3D</b><small>Ocean Intelligence Platform</small></div></div><Select label="VARIABLE" value={s.variable} values={vars} onChange={v=>s.set('variable',v as any)}/><Select label="TIME" value={times[s.timeIndex]} values={times} onChange={v=>s.set('timeIndex',times.indexOf(v))} icon={<Calendar size={15}/>}/><div className="user"><i>OS</i><div>Oceanographer<small>INCOS</small></div><ChevronDown size={15}/></div></header>}
function Select({label,value,values,onChange,icon}:{label:string,value:string,values:string[],onChange:(v:string)=>void,icon?:React.ReactNode}){return <label className="select"><small>{label}</small><span>{value}{icon}<select value={value} onChange={e=>onChange(e.target.value)}>{values.map(v=><option key={v}>{v}</option>)}</select></span></label>}
