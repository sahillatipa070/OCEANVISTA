export function exportCSV(id:string, rows:any[], m:any){const lines=['OceanVista 3D Validation Report',`Instrument,${id}`,`Generated,${new Date().toISOString()}`,'Depth (m),Observed (°C),Model (°C),Diff (°C)',...rows.map(r=>`${r.depth},${r.observed},${r.model},${(r.model-r.observed).toFixed(2)}`),'',`Bias,${m.bias.toFixed(2)}`,`MAE,${m.mae.toFixed(2)}`,`RMSE,${m.rmse.toFixed(2)}`,`Correlation,${m.correlation.toFixed(2)}`]; const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([lines.join('\n')],{type:'text/csv'}));a.download=`OceanVista_${id}_validation.csv`;a.click();URL.revokeObjectURL(a.href)}
